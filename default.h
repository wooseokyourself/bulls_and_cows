#ifndef __DEFAULT_H_
#define __DEFAULT_H_

#include <iostream>
#include <vector>
#include <ctime>
#include <cstdlib>
#include <string>

using namespace std;

enum{
    EASY = 3,
    NORMAL = 4,
    HARD = 5,

    STRIKE = 10,
    BALL = 11,
    OUT = 12,

    MAX_GAME_SET = 9
};

#endif